;-----------------------------------------------------------------------------
;--------------åéÑìãú êàëéÇÄçàü äçéèäà áÄäêõíàü éäçÄ--------------------------
;-----------------------------------------------------------------------------
.MODEL SMALL
.STACK 100H
.DATA
 GLOBAL PAR1:WORD
 GLOBAL PAR3:WORD
 GLOBAL CLOSE_WINDOW:PROC
.386
.CODE
;----------------------íÖãé åéÑìãü--------------------------------------------
CLOSE_WINDOW PROC
              XOR SI,SI
              XOR DI,DI
              MOV DI,WORD PTR PAR1[SI]   
              DEC DI
              IMUL DI,160
              MOV AX,WORD PTR PAR3[SI]      
              DEC AX
              IMUL AX,2
              ADD DI,AX
              ADD DI,122    
              ;---------------------------------------------------------------
              MOV AH,7
              MOV AL,5BH
              MOV ES:[DI],AX
              ;---------------------------------------------------------------
              MOV AH,7
              MOV AL,254
              MOV ES:[DI+2],AX
              ;---------------------------------------------------------------
              MOV AH,7
              MOV AL,5DH
              MOV ES:[DI+4],AX
              ;---------------------------------------------------------------
              RET
CLOSE_WINDOW ENDP
;-----------------------------------------------------------------------------
END
